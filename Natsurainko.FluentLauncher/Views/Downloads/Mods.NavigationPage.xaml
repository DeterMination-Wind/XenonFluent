<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Natsurainko.FluentLauncher.Views.Downloads.Mods.NavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:download="using:Natsurainko.FluentLauncher.Services.Download"
    xmlns:local="using:Natsurainko.FluentLauncher.Views.Downloads.Mods"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="30,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,12"
            Style="{ThemeResource TitleTextBlockStyle}"
            Text="Mindustry Mods" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="160" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="SearchTextBox"
                Grid.Column="0"
                Margin="0,0,12,0"
                PlaceholderText="Search GitHub for Mindustry mods..."
                Text="{x:Bind VM.SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ComboBox
                Grid.Column="1"
                HorizontalAlignment="Stretch"
                ItemsSource="{x:Bind VM.SortOptions}"
                SelectedItem="{x:Bind VM.Sort, Mode=TwoWay}" />
        </Grid>

        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,8"
            Orientation="Vertical"
            Spacing="6">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <ProgressRing
                    Width="20"
                    Height="20"
                    IsActive="{x:Bind VM.IsLoading, Mode=OneWay}"
                    Visibility="{x:Bind VM.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
                <TextBlock
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                    Text="{x:Bind VM.ErrorMessage, Mode=OneWay}"
                    Visibility="{x:Bind VM.HasError, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
            </StackPanel>

            <!-- Mod download progress / speed -->
            <Grid
                ColumnSpacing="8"
                Visibility="{x:Bind VM.IsDownloading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="{x:Bind VM.ActiveDownloadRepo, Mode=OneWay}"
                    TextTrimming="CharacterEllipsis" />
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{ThemeResource CaptionTextBlockStyle}">
                    <Run Text="{x:Bind VM.DownloadProgressText, Mode=OneWay}" />
                    <Run Text="·" />
                    <Run Text="{x:Bind VM.DownloadSpeed, Mode=OneWay}" />
                </TextBlock>
                <ProgressBar
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Maximum="1"
                    Value="{x:Bind VM.DownloadProgress, Mode=OneWay}" />
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock
                    Margin="0,12,0,0"
                    HorizontalAlignment="Center"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Text="No results"
                    Visibility="{x:Bind VM.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

                <ItemsRepeater ItemsSource="{x:Bind VM.Mods, Mode=OneWay}">
                    <ItemsRepeater.Layout>
                        <StackLayout Spacing="8" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="download:MindustryModRepo">
                            <Border
                                Padding="16"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{x:Bind FullName}" />
                                        <TextBlock
                                            MaxLines="3"
                                            Style="{ThemeResource BodyTextBlockStyle}"
                                            Text="{x:Bind Description}"
                                            TextWrapping="Wrap" />
                                        <StackPanel Orientation="Horizontal" Spacing="16">
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <FontIcon FontSize="12" Glyph="&#xE735;" />
                                                <TextBlock
                                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                                    Text="{x:Bind StargazersCount}" />
                                            </StackPanel>
                                            <TextBlock
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Style="{ThemeResource CaptionTextBlockStyle}"
                                                Text="{x:Bind UpdatedAtDisplay}" />
                                            <TextBlock
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Style="{ThemeResource CaptionTextBlockStyle}"
                                                Text="{x:Bind Language}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        Orientation="Horizontal"
                                        Spacing="8">
                                        <Button
                                            Click="OpenInBrowserButton_Click"
                                            Content="Open GitHub"
                                            Tag="{x:Bind}" />
                                        <Button
                                            Click="DownloadButton_Click"
                                            Content="Download latest release"
                                            Style="{ThemeResource AccentButtonStyle}"
                                            Tag="{x:Bind}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
